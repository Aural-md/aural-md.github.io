# Aural GitHub Pages Site

This site now uses Jekyll with the `jekyll-theme-minimal` theme.

## Local development

```sh
bundle install --path vendor/bundle
bundle exec jekyll serve
```

The homepage content lives in `index.md`, and site settings are in `_config.yml`.
